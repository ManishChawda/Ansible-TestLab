all:
  children:
    nas:
      hosts:
        qnap:
          ansible_host: 172.16.1.9
          ansible_user: admin
          ansible_password: 3@McP@%^
          ansible_connection: ssh
          ansible_shell_type: sh
          ansible_python_interpreter: /bin/false  # prevents Ansible from trying to run Python
